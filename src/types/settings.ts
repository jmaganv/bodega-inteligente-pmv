export interface Settings {
  bodegaName: string;
  ownerName: string;
  phoneYape: string;
  phonePlin: string;
  whatsappNumber: string;
  yapeQRUrl?: string; // Base64 or local image URL
  plinQRUrl?: string; // Base64 or local image URL
}
